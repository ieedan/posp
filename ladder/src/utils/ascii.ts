/* ASCII Representations for logix components */

const XIC = "─┤ ├─";

const XIO = "─┤/├─";

const OTE = "─( )─";

const OTL = "─(L)─";

const OTU = "─(U)─";

const ONS = "─[ ]─";

const NOP = "─NOP─";

const RNG = "─────";

export { XIC, XIO, OTE, OTL, OTU, ONS, RNG as RUNG, NOP };
