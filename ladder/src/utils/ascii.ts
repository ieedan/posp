/* ASCII Representations for logix components */

const XIC = "─┤ ├─";

const XIO = "─┤/├─";

const OTE = "─( )─";

const OTL = "─(L)─";

const OTU = "─(U)─";

const ONS = "─[ ]─";

const NOP = "─NOP─";

const RNG = "─────";

export { NOP, ONS, OTE, OTL, OTU, RNG as RUNG, XIC, XIO };
